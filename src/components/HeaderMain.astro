---
import { Icon } from 'astro-icon';
export interface Props {
    title: string;   
}
const {title} = Astro.props;
---
<div class="h-20 bg-black bg-opacity-50 shadow-xl fixed w-full z-10 taos:bg-opacity-50" >
    <div class="flex items-center h-20 px-5">
        <h1 class="flex-auto text-2xl">
            <span class="duration-[1000ms] [animation-iteration-count:infinite] taos:opacity-0" >{title}</span>
            </h1>    
        <div class="flex justify-end">
            <div class="w-10 sm:hidden">
                <Icon name="ic:round-menu-open"></Icon>
            </div>           
            <div class="w-full hidden sm:block">
                <a class="px-1 hover:scale-125 hover:cursor-pointer" href="#Inicio">Inicio</a>
                <!--a class="px-1 hover:scale-125 hover:cursor-pointer" href="#Proyectos">Proyectos</a>
                <a class="px-1 hover:scale-125 hover:cursor-pointer">Estudios</a>
                <a-- class="px-1 hover:scale-125 hover:cursor-pointer">Contáctame</a-->
            </div>             
        </div>
        <div class="text-xs bg-blue-500 p-1 rounded-md ml-3 items-center hover:cursor-pointer">
            <span>EN</span>
        </div>            
    </div>
    
</div>
<div class="h-20"></div>